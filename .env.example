# Optional: Grok (xAI) API Key for "Corporate Polish" story rephrasing on /submit (falls back to local filter if unset)
GROK_API_KEY=
GEMINI_API_KEY=

# Optional: 100% Free Cloud Database (Turso / LibSQL). 
# If left blank, it automatically uses the local ghosted.db SQLite file for $0.
DATABASE_URL=
DATABASE_AUTH_TOKEN=
